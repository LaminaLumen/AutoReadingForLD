# 更新日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## 2.1.3 - 2026-09-25

### 长帖阅读记录

- 修复蓝点短暂未消失就被判定失败的问题：澜阅会守住当前未确认楼层，给 Discourse 原生阅读计时的限流或故障退避留出最多 120 秒，不会跨过待确认楼层。
- 针对已实测的 `/topics/timings` 429：被动读取浏览器中同源请求的状态码；收到 429 立即停止滚动并保留当前楼层，站点原生重试成功后才恢复，短期内降低实际滚动上限；持续限流最多等待 10 分钟后停在当前帖并停止连续队列。
- 等待期间保持有限次原生微滚动，使站点追踪器持续感知当前视口；蓝点消失后立即续读。页面失去焦点时不消耗确认超时时间。
- 无 429 信号但原生重试期结束仍未确认时，才完整重载当前楼层一次；重载后等待目标楼层实际渲染并定位成功再恢复，避免在占位内容上提前开滚。
- 重载后仍未确认会停在当前帖并停止连续队列，保留明确的失败状态，不伪造站点阅读响应。
- 验证夹具新增高楼层编号、单楼延迟消点和真实 HTTP 429→200 / 持续 429 场景；脚本仍不自行发送阅读记录。

## 2.1.2 - 2026-09-23

### 连续阅读与楼层加载

- 连续读改为滑动队列：默认保持 10 篇待读帖，可调 1–20 篇；剩余约 2 篇时返回原列表，按原顺序自动补满，并记住本轮已收集的话题，避免重复阅读。
- 从旧版默认的 8 篇设置迁移到 10 篇；运行中调整队列容量或本轮上限时，新的设置从下一轮开始生效。
- 新增独立的本轮总上限，默认 100 篇、可调 10–200 篇；补队列通过站点原生列表加载哨兵有限次翻页，列表耗尽或达到上限后才正常完成。
- 补队列和冷却阶段支持刷新恢复；离开原列表、主动停止或阅读记录持续未确认时停止，不会带着未确认楼层切换下一帖。
- 连续读在所有支持的列表中仅收集带站点未看/未读标记、且未被标记为已访问的帖子；已有新回复的旧帖也不再入队。
- 当前帖、自动帖和连续读共用原生楼层预加载：阅读时提前移动 Discourse 的隐藏加载哨兵，让站点在抵达已加载末楼前请求后续批次，不改变楼层顺序或站点每批大小。
- 已加载楼层末端仍有后续内容时保持“载入”，不再把当前批次误判为整篇完成；后续内容长时间无响应时显示加载超时并停在当前帖。
- 连续读只在正文流确认已到末楼时根据剩余页面距离限速；超长帖未加载完成时维持用户设定的平均速度，最低阅读时长仍在真正末尾生效。
- 未读楼层即将离开视口时才停留确认，减少蓝点已在正常滚动中消失时的无谓等待；离帖前确认与单次恢复规则保持生效。
- 本地夹具增加混合已读列表、列表分批补充、正文分批加载、超长帖和加载停滞场景。

## 2.1.1 - 2026-09-22

### 阅读记录

- 新增逐视窗阅读确认：当 Discourse 未读蓝点进入中央阅读区时，按同屏楼层成组停留 2.8–4.2 秒，蓝点提前消失则立即继续。
- 确认期间使用一次 `1px` 真实微滚动重新触发 Discourse 可见楼层观察器，不直接调用或伪造阅读上报。
- 新增离帖前记录闸门：到达末尾后等待蓝点消失并稳定 0.9 秒，与懒加载等待和楼层阅读计划同时生效。
- 连续两批可见楼层仍未确认时，只重载当前帖当前楼层一次并自动恢复；重载后仍失败则停在本帖并停止连续队列，不会反复刷新或跳过未确认楼层。
- 本地验证页新增蓝点延迟消失与永不消失两种模拟，覆盖正常续滚、单次重载恢复和失败停队列。

## 2.1.0 - 2026-09-22

### 品牌

- 项目正式更名为“澜阅”，产品副标题为“LINUX DO 沉浸阅读器”，仓库更名为 `Lanyue`。
- 发布文件改为 `Lanyue.user.js`，脚本名称、项目主页、问题反馈、下载地址和更新地址全部切换到新仓库。
- 澜阅作为新的用户脚本发布，不保留 AutoReadingForLD 发布文件；旧版用户需要停用旧脚本并重新安装。

### 设计

- 建立以水波线条为识别元素的澜阅品牌标识，替换原有工具化标题。
- 细化模式图标容器、标题层级、面板表面、主操作按钮和收起状态文案，在保持 `336px` 小尺寸布局的同时提升辨识度与质感。
- 重写就绪、阅读、加载、等待、暂停与完成状态文案，使状态表达更短、更直接。
- 将收起态升级为 `148 × 48px` 快捷胶囊，主区可直接开始、暂停、继续或重读，右侧参数按钮独立展开完整设置，拖动主区不会误触发操作。
- 新增跟随网页主题和阅读状态的 WebGL 水纹材质，并限制动态帧率、支持后台停绘、减少动态效果和 CSS 自动降级。

### 连续阅读

- 根据帖子总楼层数为每篇安排 30 秒至 10 分钟的最低有效阅读时长，并兼容帖子进度、时间线、已加载楼层与 Discourse 预载数据。
- 高速档会按剩余距离与剩余计划时间柔和限速，原本更慢的阅读节奏不会被强制加快。
- 提前到达底部时显示计划剩余时间；暂停、后台冻结不消耗有效时长，懒加载发现更多楼层后计划只延长不缩短。
- 按浏览器实际滚动位移回补像素量化误差，修复高 DPI 或页面缩放环境中实际速度高于设置值的问题。

## 2.0.2 - 2026-09-22

### 修复

- 修复 Discourse 随阅读进度更新地址中的楼层号时，被误判为离开当前帖并重置滚动的问题；路由变化现在按帖子 ID 判断，同一帖内定位不会中断。
- 修复后台暂停只会停止、返回页面却不会继续的问题；由页面可见性触发的暂停现在会在返回原帖子后自动恢复。
- 将后台暂停与手动操作暂停改为默认关闭，并迁移仍使用 2.0.0/2.0.1 整组旧默认值的配置，避免自动帖和连续读频繁意外中断。
- 手动暂停只响应真实用户输入，并在刚开始阅读后的短暂时间内忽略触控板惯性事件。
- 连续阅读改用可由 Discourse 接管的同源链接完成换帖和返回列表，使站点路由在离开帖子前有机会刷新原生阅读计时。

### 说明

- 脚本仍不伪造或直接调用阅读上报接口；阅读记录由 LINUX DO / Discourse 按页面可见性、楼层曝光和停留时间自行判定。

## 2.0.1 - 2026-09-22

### 修复

- 修复平均速度改为 `px/s` 后，低于每帧 1 像素的位移被浏览器持续取整为 0，导致界面显示“阅读中”但页面不滚动的问题。
- 新增小数像素累积器，使沉浸、标准、速览及自定义速度在不同刷新率下都能产生稳定的实际滚动。
- 默认速度调整为 `30 px/s`，与原版最低档 `0.5 px/frame` 在常见 60Hz 环境下的速度一致；已有 2.0.0 旧默认值会自动迁移，其他自定义速度保持不变。
- 细化速度滑块粒度，确保默认 `30 px/s` 同时也是滑块的真实选中值。
- 修复网页背景透明时被当作黑色处理，导致浅色 LINUX DO 页面错误显示深色控制台的问题。

### 改进

- 控制台改为读取 Discourse 的背景、文字、边线与强调色主题令牌，并监听站内主题样式表、主题属性及系统配色变化。
- 将原 `46px` 书本入口重构为 `116 × 44px` 状态胶囊，以进度环、动态图标和中文说明直接区分待机、阅读中、等待、队列、暂停及完成状态。

## 2.0.0 - 2026-09-22

### 新增

- 以 AutoReadingForLD 名称重新组织项目和用户脚本元数据。
- 新增“当前帖、自动帖、连续读”三种阅读模式。
- 新增有篇数上限的连续阅读队列，支持在新帖、未读、未看和最新列表建立队列。
- 新增每篇之间 8 秒前台冷却、队列恢复、偏离队列自动停止和完成后返回列表。
- 新增沉浸、标准、速览三档阅读节奏。
- 速度设置改为平均速度，并加入 ±12% 的缓慢自然波动。
- 新增阅读进度、有效用时和滚动行程统计。
- 新增手动滚动/触摸/翻页时自动暂停选项。
- 新增暗色主题、键盘焦点样式和减少动态效果支持。
- 新增全中文阅读控制台，采用暖纸白、深林绿与低饱和赤陶色，并为窄屏提供面板内滚动。
- 新增小尺寸“书页边注”双栏布局，以左侧模式轨承载当前帖、自动帖和连续读。
- 新增统一的 Phosphor Icons 线性图标体系，覆盖模式、主操作、参数、统计、阅读保护与位置操作。
- 新增带页面进度环和状态点的 `46px` 收起入口；入口可在整个视口内自由移动。
- 新增四象限自适应展开：根据收起入口位置自动向内展开，并保持对应角锚点稳定。
- 新增 GitHub Raw 一键安装与自动更新地址。
- 新增项目检查脚本、开发文档、设计文档和发布指南。

### 重构

- 将滚动速度从 `px/frame` 改为 `px/s`，消除不同刷新率设备上的速度差异。
- 将底部等待从帧数计时改为单调时钟计时，避免后台降帧或高刷屏造成误判。
- 使用 Pointer Events 统一鼠标、触控笔和触摸拖拽逻辑。
- 拖拽与展开改用独立布局坐标，不受 CSS 动画、后台降帧或减少动态效果影响。
- 使用 Shadow DOM 隔离控制台结构与样式，降低与 Discourse 主题冲突的概率。
- 重构为明确的存储、拖拽、滚动、路由与视图状态模块。
- 重排控制台信息层级，将模式、平均速度、队列设置、统计与阅读保护按操作频率组织。
- 限定脚本只匹配 `/t/`、`/n/` 帖子页与 `/new`、`/unread`、`/unseen`、`/latest` 列表页，并兼容 Discourse 单页导航。

### 安全

- 权限调整为 `@grant none`。
- 不包含跨域访问、远程依赖、Cookie 操作或第三方数据上报。
- 连续队列恢复时强制校验同源帖子与列表地址，避免异常本地数据触发站外导航。
- 不包含自动点赞、自动回复、自动收藏、随机点击或风控规避能力。

### 兼容

- 首次运行时可迁移旧版 `linuxdo-autoscroll-settings` 中的速度和最小化状态。
