# 参与贡献

感谢关注 AutoReadingForLD。提交变更前，请遵循以下约定。

## 项目边界

欢迎贡献以下类型的改进：

- 自动滚动的稳定性、性能和浏览器兼容性；
- LINUX DO / Discourse 页面结构兼容；
- 三种阅读模式、有界队列及恢复机制的可靠性；
- 控制台可访问性、响应式布局与主题适配；
- 文档、测试和发布流程；
- 不扩大权限范围的本地设置能力。

项目不接受以下功能：

- 自动点赞、自动回复、自动收藏、随机点击或自动互动；
- 无上限翻页、无冷却连续遍历或其他不可控的批量行为；
- 绕过社区限制、风控或信任等级规则；
- 读取或外传 Cookie、Token、账号资料和帖子内容；
- 无必要的第三方依赖、远程脚本或全站匹配权限。

## 开发流程

1. 从 `main` 创建语义清晰的分支。
2. 修改 `AutoReadingForLD.user.js` 及相关文档。
3. 版本变化时同步更新元数据、`package.json` 和 `CHANGELOG.md`。
4. 运行 `npm run check`。
5. 在亮色、暗色、长帖、列表、三种阅读模式及窄屏环境中进行人工验证。

提交信息应简洁说明实际变化。代码注释优先使用中文，只解释关键规则、兼容性处理和不直观的边界条件。
