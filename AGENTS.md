# 澜阅协作约定

## 项目边界

- 根目录 `Lanyue.user.js` 是唯一发布脚本，不引入构建产物或远程运行时依赖。
- 只匹配 LINUX DO 的 `/t/`、`/n/` 帖子页和 `/new`、`/unread`、`/unseen`、`/latest` 列表页。
- 保持 `@grant none`，不得新增跨域请求、Cookie/Token 读取、远程脚本或全站匹配权限。
- 连续阅读使用默认 10 篇的滑动队列与独立的本轮总上限，同标签页运行并保持可见停止入口；只从原列表有限次补充，不实现随机点击、自动互动、无限遍历或风控规避。

## 界面约定

- 使用中文界面与暖纸白、深林绿、灰鼠尾草、低饱和赤陶配色。
- 展开态基准宽度 `336px`；收起态为 `148 × 48px` 快捷胶囊，主区控制阅读，右侧入口展开设置。
- 收起入口可在完整视口移动；展开方向按所在象限向内适配，并使用布局坐标保持锚点稳定。
- 功能图标沿用 Phosphor Icons 2.1.1 Regular，不混用 Emoji、字符图标或其他视觉风格。
- 控制台继续使用 Shadow DOM；新增控件必须包含键盘焦点和可访问名称。

## 开发与验证

- 代码注释、README、设计说明和发布文档优先使用中文。
- 修改脚本后运行 `npm run check`。
- 本地预览使用 `npm run preview`，刷新页面即可读取最新脚本。
- 涉及布局或状态的改动至少验证亮色、暗色、窄屏、三种阅读模式、展开/收起和浏览器控制台错误。
- 版本发布时同步更新用户脚本元数据、运行时版本、`package.json` 与 `CHANGELOG.md`。
- Discourse 会在同一帖子滚动期间把 URL 更新为带楼层号的路径；路由状态只能按视图类型与帖子 ID 判断，不得用完整 URL 变化直接重置滚动。
- 阅读记录由 Discourse 的可见楼层与停留计时机制原生提交；脚本只读取本地 DOM 和浏览器暴露的同源请求状态元数据，产生原生滚动并在有界恢复时重载当前楼层，不拦截、直接调用或伪造 `/topics/timings`。
- 阅读记录确认以 `.read-state:not(.read)` 为未读信号，在楼层即将离开视口时按同屏楼层成组处理；蓝点消失后及时续滚，不得对每楼无界等待。
- 蓝点未消失时应守住当前楼层，等待 Discourse 原生计时请求的有界重试（当前最多 120 秒）。明确检测到 429 时停止滚动，等待站点原生重试成功后降速续读；持续限流最多 10 分钟后停在当前帖并停止队列，不借重载或新会话规避限流。无 429 的恢复只能使用有限原生微滚动和每话题一次受控重载；重载后必须等目标楼层实际渲染再恢复。
- 三种模式通过 Discourse 原生 `.post-stream > .load-more-sentinel` 提前加载后续楼层；只移动隐藏哨兵的视觉位置，不跳读、不自行拉取楼层数据或修改站点批次大小。哨兵仍存在时不能判定整帖完成。
- 连续读只收集带站点未看/未读标记且未访问过的列表行；已访问旧帖即使有新回复也不入队。
- 补队列必须记住本轮已收集的话题 ID；只在原列表通过原生列表加载哨兵寻找候选，刷新可恢复，耗尽或达到本轮上限后结束，不得循环读取同一帖。
- 连续阅读按楼层数规划最低有效时长；未加载完整篇时不得用当前页面剩余高度摊算全篇速度，真正到末楼后高速档才允许向下限速，懒加载后计划只能延长。
